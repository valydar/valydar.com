name: valydar_flutter
description: Valydar Identity Verification SDK for Flutter
version: 0.1.0
homepage: https://valydar.com

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
